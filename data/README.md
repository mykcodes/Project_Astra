# Local Data

This directory stores runtime data for the local instance of ASTRA.
**This entire directory is gitignored.**

Subdirectories (created at runtime):
- `documents/`: Ingested knowledge documents.
- `uploads/`: Temporary user uploads.
- `cache/`: Audio and API response caching.
- `local/`: Local SQLite/DuckDB databases if used for tools.
