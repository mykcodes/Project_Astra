# Shared Contracts

This directory is the future home for shared contracts between the frontend and backend.

When the project matures, API schemas, event types, and constants defined here will be used to generate TypeScript interfaces and Pydantic models automatically, ensuring the frontend and backend remain perfectly in sync.
